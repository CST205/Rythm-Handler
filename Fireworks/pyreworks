import pygame

pygame.init()

canvas = pygame.display.set_mode((800,600))
pygame.display.flip()

running = true
while running:
    for event in pygame.event.get():
        if even.type == pygame.QUIT:
            running = false

    
    

pygame.quit()
quit()
